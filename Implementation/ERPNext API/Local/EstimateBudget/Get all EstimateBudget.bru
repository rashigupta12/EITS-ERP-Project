meta {
  name: Get all EstimateBudget
  type: http
  seq: 3
}

get {
  url: http://eits.local:8000/api/resource/EstimateBudget?fields=["estimate_range","from", "to"]
  body: none
  auth: inherit
}

params:query {
  fields: ["estimate_range","from", "to"]
}
